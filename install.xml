<?xml version="1.0" encoding="iso-8859-1" standalone="yes" ?>

<!-- 
    A sample installation file.
    Use it as a base for your own installers :-)
    
    To compile it :
    - go in the bin directory where you installed IzPack
    - call "compile ../sample/install.xml -b ../sample"
-->

<installation version="1.0">

    <!-- 
        The info section.
        The meaning of the tags should be natural ...
    -->
    <info>
        <appname>Next ESA SAR Toolbox</appname>
        <appversion>1B</appversion>
        <url>http://www.array.ca/nest</url>
    </info>
    
    <!-- 
        The gui preferences indication.
        Sets the installer window to 640x480. It will not be able to change the size.
    -->
    <guiprefs width="640" height="480" resizable="no"/>
    
    <!-- 
        The locale section.
        Asks here to include the English and French langpacks.
    -->
    <locale>
        <langpack iso3="eng"/>
        <langpack iso3="fra"/>
		<langpack iso3="ita"/>
		<langpack iso3="spa"/>
		<langpack iso3="deu"/>
    </locale>
    
    <!-- 
        The resources section.
        The ids must be these ones if you want to use the LicencePanel and/or the InfoPanel.
    -->
    <resources>
        <res id="LicencePanel.licence" src="LICENSE.txt"/>
        <res id="InfoPanel.info" src="readme.txt"/>
    </resources>
    
    <!-- 
        The panels section.
        We indicate here which panels we want to use. The order will be respected.
    -->
    <panels>
        <panel classname="HelloPanel"/>
        <panel classname="InfoPanel"/>
        <panel classname="LicencePanel"/>
        <panel classname="TargetPanel"/> 
        <panel classname="PacksPanel"/>
        <panel classname="InstallPanel"/>
        <panel classname="FinishPanel"/>
    </panels>
    
    <!-- 
        The packs section.
        We specify here our packs.
    -->
    <packs>
        <pack name="Base" required="yes">
            <description>The base files</description>
			<file src="uninstall.bat" targetdir="$INSTALL_PATH"/>
			<file src="uninstall.sh" targetdir="$INSTALL_PATH"/>
			<file src="target/nest-1B-bin.dir" targetdir="$INSTALL_PATH"/>
        </pack>
        <pack name="Docs" required="no">
            <description>The documentation</description>
            <file src="readme.html" targetdir="$INSTALL_PATH"/> <!-- Reccursive adding -->
        </pack>
        <pack name="Sources" required="no">
            <description>The sources</description>
            <file src="target/nest-1B-src.zip" targetdir="$INSTALL_PATH"/>
        </pack>
    </packs>
    
</installation>
